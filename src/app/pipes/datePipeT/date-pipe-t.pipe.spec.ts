import { DatePipeTPipe } from './date-pipe-t.pipe';

describe('DatePipeTPipe', () => {
  it('create an instance', () => {
    const pipe = new DatePipeTPipe();
    expect(pipe).toBeTruthy();
  });
});
